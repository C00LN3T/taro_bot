# Telegram-бот «Гадалка» — техническое задание

## 1. Общие сведения о проекте
- **Название:** Telegram-бот «Гадалка»
- **Цель:** предоставлять пользователю быстрые гадательные и самопознательные расклады (таро, нумерология, астрология/космо и др.) в удобной форме в Telegram.
- **Формат:** Telegram Bot API, одно- или двуязычный интерфейс (обязательный русский, опционально английский).

## 2. Основной функционал бота
### 2.1. Типы раскладов (минимальный набор)
- **Таро**
  - Однокарточный «совет дня»
  - Три карты (прошлое / настоящее / будущее)
  - Расклад на ситуацию (3–5 карт)
  - Любовный расклад (3–5 карт)
  - Карьера/финансы (3–5 карт)
- **Нумерология**
  - Число судьбы (по дате рождения)
  - Число имени
  - Краткая нумерологическая карта личности (3–5 ключевых чисел)
  - Совместимость по датам рождения (2 человека)
- **Астрология / космо**
  - Солнечный знак зодиака по дате рождения
  - Краткий астропортрет (по знаку)
  - «Космограмма-лайт» (основные качества по знаку/стихии/модальности; без реальных эфемерид)
- **Прочее (опционально)**
  - Руны (1 или 3 руны)
  - Метафора дня (случайная фраза/картинка)

### 2.2. Сценарии работы пользователя
- **Первый запуск**: приветственное сообщение, краткое объяснение, выбор языка (если включён), запрос имени/ника, даты рождения и пола (опционально), сохранение профиля, показ главного меню.
- **Главное меню (ReplyKeyboard)**: 🔮 Таро, 🔢 Нумерология, 🪐 Астрология / Космо, 🎁 Случайный расклад, ⭐ Мой профиль, ℹ️ Помощь.
- **Запрос расклада**: выбор раздела → тип расклада → сбор дополнительных данных (дата рождения, второй человек и т.п.) → генерация результата (текст +, опционально, изображение) → кнопки: «Сделать ещё один», «Поделиться», «Вернуться в меню».
- **Профиль пользователя**: отображение данных (имя, дата рождения и т.п.); кнопки: «Изменить дату рождения», «Изменить язык», «Удалить данные» (GDPR-подход).

## 3. Функциональные требования
### 3.1. Взаимодействие с пользователем
- Поддерживаемые типы сообщений: текст + кнопки (ReplyKeyboard / InlineKeyboard), опционально стикеры/эмодзи.
- Команды: `/start`, `/help`, `/menu`, `/profile`, `/language`.
- Ответы дружелюбные и кратко поясняют действия.

### 3.2. Логика генерации раскладов
- **Таро**: псевдослучайный выбор требуемого количества карт; случайное положение (прямое/перевёрнутое); формирование текста: название расклада + список карт + интерпретация.
- **Нумерология**: расчёты по фиксированным формулам; тексты интерпретаций из справочников; поддержка нескольких типов расчётов.
- **Астрология/космо**: определение знака зодиака по дате рождения; справочник «знак → описание/совет»; возможность расширения (стихии, совместимости).
- **Случайный расклад**: случайный выбор одной из доступных систем и типов с учётом разрешений (например, только бесплатные).

### 3.3. Монетизация (опционально)
- **Бесплатно:** ограничение N раскладов в день, базовые расклады.
- **Премиум:** безлимитные расклады, сложные/длинные расклады, персональные сохранённые расклады; интеграция с Telegram Payments или внешним платёжным сервисом; лимиты чётко описываются (пример: 5 бесплатных раскладов в день на пользователя).

## 4. Нефункциональные требования
### 4.1. Технологический стек (рекомендация)
- Язык: Python; фреймворк: `aiogram` или `python-telegram-bot`; БД: PostgreSQL (либо SQLite для MVP); хостинг: VPS/Docker/Heroku-аналог.

### 4.2. Производительность
- Одновременная поддержка целевого числа активных пользователей (например, 1000).
- Среднее время ответа: ≤2–3 секунды при нормальной нагрузке.

### 4.3. Надёжность
- Логирование: входящие сообщения, исключения, критические события (недоступность БД, внешние сервисы).
- Автоперезапуск процесса: systemd / supervisord / pm2 / Docker restart policy.

### 4.4. Безопасность и конфиденциальность
- Секреты в переменных окружения.
- Минимум собираемых данных (имя, дата рождения, Telegram ID).
- Возможность полного удаления данных пользователя по запросу.

## 5. Структура данных (БД, пример схемы)
- **users**: `id` (PK), `telegram_id` (unique), `username`, `first_name`, `birth_date` (DATE, nullable), `gender` (nullable), `language` (ru/en), `created_at`, `updated_at`.
- **tarot_cards**: `id` (PK), `name`, `arcana_type` (major/minor), `suit` (wands/cups/swords/pentacles/null), `upright_meaning` (TEXT), `reversed_meaning` (TEXT), `image_url` (nullable).
- **numerology_texts**: `id`, `number`, `type` (destiny/life_path/name и т.п.), `description`.
- **zodiac_signs**: `id`, `name`, `date_start`, `date_end`, `description`.
- **sessions** (опционально): `id`, `user_id`, `current_state`, `payload` (JSONB), `updated_at`.
- **spreads_history**: `id`, `user_id`, `type` (tarot/numerology/astro/other), `spread_name`, `input_data` (JSON), `result` (TEXT или JSON), `created_at`.

## 6. Машина состояний / логика диалога
- Требуется FSM или аналог для сложных сценариев.
- Примеры состояний: `STATE_IDLE`, `STATE_TAROT_CHOOSE_SPREAD`, `STATE_TAROT_CONFIRM_QUESTION`, `STATE_NUMEROLOGY_WAIT_BIRTHDATE`, `STATE_NUMEROLOGY_WAIT_SECOND_BIRTHDATE`, `STATE_ASTRO_WAIT_BIRTHDATE`, `STATE_PROFILE_EDIT_BIRTHDATE`, и т.д.
- Документировать список состояний и переходов (диаграмма или таблица) при реализации.

## 7. Административный функционал
- Конфигурирование списка доступных раскладов (вкл/выкл, описание).
- Просмотр статистики: количество пользователей, количество раскладов по типам, активность за день/неделю.
- Пример админ-команд (для списка ID админов): `/admin_stats` — краткая статистика; `/broadcast <text>` — рассылка всем пользователям (осторожно).

## 8. Текстовое наполнение
- Отдельный справочник таро: все карты + значения (прямое/перевёрнутое).
- Справочник нумерологии: описания для чисел, типов расчётов.
- Справочник астрологии: описания знаков.
- Тексты сообщений бота: приветствие, помощь, ошибки (неверный ввод), тексты кнопок и меню. Можно хранить в YAML/JSON или отдельном документе.

## 9. Тестирование и приёмка
### 9.1. Юзкейсы для тестов
- Первый запуск / смена языка.
- Ввод корректной и некорректной даты рождения.
- Все типы раскладов: корректный сценарий, отмена/возврат в меню.
- Лимиты (если включена монетизация/ограничения).
- Быстрые сериалы запросов от одного пользователя.
- Устойчивость к «мусорному» вводу (текст, эмодзи, стикеры).

### 9.2. Критерии приёмки
- Бот стабильно отвечает и не падает при стандартной нагрузке.
- Все заявленные команды и кнопки работают.
- Справочники корректно подтягиваются, нет пустых значений.
- Пользователь может из любой точки попасть в меню, изменить данные или удалить данные при желании.
